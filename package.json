{
    "name": "pi-pokemon-deploy",
    "version": "1.0.0",
    "description": "Root package.json to proxy Vercel builds to client folder",
    "scripts": {
        "build": "echo 'BUILD START' && cd client && npm install && npm run build && cd .. && echo 'MOVING BUILD' && rm -rf build && mv client/build build && echo 'BUILD DONE'"
    },
    "private": true,
    "engines": {
        "node": ">=14.0.0"
    }
}